 int findMaxConsecutiveOnes(vector<int>& nums) {
        int count =0;
        int maxi=0;
        for(int a : nums){
            if(a==1){
                count++;
                maxi=max(maxi,count);
            }
            else{
                count=0;
            }
        }
        return maxi;
    }
