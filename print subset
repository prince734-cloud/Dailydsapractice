#include <iostream>
#include<vector>

using namespace std;
void printsubset(vector<int>& nums,int i,vector<int>& ans){
    if(i==nums.size()){
        for(int val: ans){
            cout<< val <<" ";
        
        }
        cout<<endl;
        return;
    }
     ans.push_back(nums[i]);
     printsubset(nums , i+1 ,ans);
     
     ans.pop_back();
     printsubset(nums,i+1,ans);
}

int main()
{ vector<int> nums ={1,2,3};
  vector<int> ans;
  printsubset(nums,0,ans);
   
    return 0;
}
