#include <iostream>
#include <vector>
using namespace std;

class Node {
    public:
    int data;
    Node* next;
    Node(int val){
        data = val;
        next = NULL;
    }
};
class Queue{
    Node* head;
    Node* tail;
    public:
    Queue(){
        head= tail = NULL;
    }
    void push(int val){
        Node* newnode = new Node(val);
      if(head==NULL){
          head= tail=newnode;
          
      }
      else{
          tail->next=newnode;
          tail=newnode;
      }
        
    }
    void pop(){
        if(head==NULL){
            cout<<"No data is present"<<endl;
            
        }
        else{
            Node* temp = head;
            head= head->next;
            delete temp;
            
        }
    }
    int front(){
        if(isempty()){
            return -1;
        }
        return head->data;
        
    }
    bool isempty(){
        return head==NULL;
        
    }
};

int main()
{ Queue q;
 q.push(19);
 q.push(26);
  while(!q.isempty()){
      cout<<q.front()<<" ";
      q.pop();
  }
    

    return 0;
}
