class Solution {
public:
    int product(int n) {
        if (n < 10) return n;
        return (n % 10) * product(n / 10);
    }

    int smallestNumber(int n, int t) {
        while (product(n) % t != 0)
            n++;
        return n;
    }
};
